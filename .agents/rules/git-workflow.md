# Git Workflow & Commit Policy

Always commit and push changes after implementing and verifying user requests.

- Stage modified and new source files: `git add <files>`
- Create a clear, concise conventional commit: `git commit -m "..."`
- Push directly to the active remote branch: `git push origin <branch>`
